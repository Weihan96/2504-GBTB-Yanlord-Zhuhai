# IFC 几何、施工模度与原点归整标准

> 项目：2504 GBTB Yanlord Zhuhai
> 状态：项目执行标准
> 创建时间：2026-08-04 17:53 +08:00
> 适用范围：墙、门窗、Space、地面、天花及固定施工构件
> 人眼审核界面：Blender + Bonsai Solid View

## 1. 目标

在不猜测设计意图、不破坏对象关系的前提下，使已确认施工几何具备稳定的毫米级控制尺寸，并让 IFC `ObjectPlacement` 原点落在可解释的几何锚点上。归整结果必须可重复、可验证、可回退，并能直接服务 A-103、A-104、A-105 与后续施工图编译。

## 2. 权威数据与边界

1. IFC 保存已确认几何和语义；同一批次只能有一个 IFC 写入者。
2. 本文档保存几何归整规则和验收门，不保存逐对象审核结果。
3. `pipeline/decisions/p0-review.csv` 保存需长期追踪的决策、依据、置信度和状态。
4. `build/**` 保存可重建的对象清单、dry-run、差异和 QA 报告；它不是源数据。
5. Blender 是唯一人眼审核界面；机器 CSV/JSON 不要求审核人直接阅读。
6. IDS 只验证 IFC 信息要求，不能表达墙体模度、几何锚点或相邻构件联动规则。
7. occurrence 未单独写 `PredefinedType` 时，必须先读取其关联 `IfcTypeProduct.PredefinedType`；类型已给出明确非 `NOTDEFINED/USERDEFINED` 语义的对象不得再仅凭几何方向送人审，也不重复写 occurrence 字段。

## 3. 数值规则

| 类别 | 规则 | 自动化边界 |
| --- | --- | --- |
| 浮点尾差 | 距离整数不超过 `0.01 mm` 的 `IfcLengthMeasure` 可写为精确整数 | 可在候选副本验证后受控写入 |
| 参数 JSON 尾差 | Bonsai 门窗参数 JSON 中距离整数不超过 `0.01 mm` 的长度值可写为精确整数 | 比例和非近整数数值保持；必须证明门窗世界几何不变 |
| 对象原点尾差 | 原点单轴距离整数不超过 `0.1 mm` 可归整 | 必须保持父子关系并保存、重载、复核 |
| 剩余对象原点 | 单轴超过 `0.1 mm` 使用红色标记 | 不得仅因小于 `0.5 mm` 而批量移动对象 |
| 施工参数精度 | 墙长、墙厚、墙高、标高、地面和天花控制尺寸采用 `1 mm` 精度 | 只有确认的控制参数可写入 |
| 优先模度 | 优先采用 `100 mm` 模度 | 不是无条件四舍五入规则 |
| 例外模度 | `50 mm`、`30 mm`、`20 mm` 可作为有依据的例外 | 必须记录几何、Grid、相邻构件或产品规格依据 |
| 板材和薄层 | 产品规格明确时允许小数毫米 | 不得用普通整数规则覆盖已确认产品规格 |
| 面积和体积 | 允许小数 | 不参与长度取整 |
| 复杂家具网格 | 保留原始小数顶点 | 除非有明确安装控制尺寸，否则不归整网格 |

## 4. 原点与几何锚点

红色审核点是 Blender 临时标记对象；它不是 IFC 几何。IFC 原点来自 `ObjectPlacement`，几何顶点、边和面位于该原点定义的局部坐标系内。

要求“原点为整数”且“原点落在几何特征上”时，必须先确认或生成整数几何锚点，再无损重设 `ObjectPlacement`。不得独立移动相邻墙、门洞、门窗或空间边界来追求表面上的整数坐标。

| IFC 类别 | 首选锚点 | 备选锚点 | 禁止默认使用 |
| --- | --- | --- | --- |
| IfcWall | 墙轴线起点与完成面标高交点 | 已确认底面轮廓角点 | 世界包围盒中心 |
| IfcBeam | 未被裁剪且落在 Body 上的 Axis 端点 | Body 顶面或物理边上的确定整数 Grid 点 | 裁剪体外的 Axis 端点、世界包围盒中心 |
| IfcDoor | 洞口中心线与门槛标高交点 | 门洞底边中点 | 门扇复杂网格质心 |
| IfcWindow | 洞口中心线与窗台标高交点 | 洞口底边中点 | 窗框详细网格质心 |
| IfcSlab / IfcCovering | 已确认边界角点与完成面标高交点 | 物理轴向边上的确定整数 Grid 点或直线边中点 | 非凸轮廓包围盒中心 |
| IfcBuildingElementProxy（明确固定天花/灯槽） | 已确认安装边界角点 | 物理轴向边上的确定整数 Grid 点 | 导入 CAD 原点、世界包围盒中心；不得把风口等机电对象仅因位于天花区并入 |
| IfcElementAssembly | 与组合用途对应的子构件安装边界点 | 子构件实体上的确定整数 Grid 点 | 无 Body 的任意组原点、子构件包围盒中心；重设后必须在候选写盘重开后证明所有后代世界 placement 和几何不变 |
| 固定机电/洁具（IfcElectricAppliance / IfcLightFixture / IfcPipeSegment / IfcSanitaryTerminal） | 已知连接点、设备中心线或安装面基准点 | Body 的既有整数顶点或物理轴向边整数点 | 世界包围盒中心；不得把备选几何点当作已确认的连接尺寸或安装定位语义 |
| IfcSpace | 地面边界上的确定角点 | 确认位于区域内的边中点 | 可能落在 L 形外部的质心 |
| 固定家具 | 安装基准角点或中心线交点 | 已验证安装表面的物理顶点、物理边中点或整数表面点 | 三角化内部点或脱离几何的任意坐标 |

锚点选择以现场安装便利为最高优先级，但“方便”必须能转化为可测量、可放线、可复核的施工基准：

1. 固定设备优先使用与支承、吊装、墙装或柜体安装直接对应的安装面中心或安装中心线；检修面只记录净空，不与安装锚点混用。
2. 卫生洁具优先使用厂家粗装定位点、给排水连接中心或固定件中心线；没有产品安装图时，不从复杂外壳网格猜连接点。
3. 管段和管件优先使用连接口中心或管道中心线交点；外表面顶点只能证明几何存在，不能替代管中心定位。
4. 台面、柜体和固定构件优先使用安装人员可从完成面、Grid 或相邻柜体直接量取的基准角点或基准边。
5. 多个候选均满足几何门时，选择现场测量链最短、与相邻安装对象联动最少的候选；选择依据写入 `p0-review.csv`。

## 5. 颜色和 Blender 审核状态

- 红色实体点：对象原点至少一轴超过 `0.1 mm`，仅表示原点问题。
- 橙色实体点：墙体拉伸深度或水平包围尺寸存在非整数候选，仅表示尺寸需复核。
- 红色和橙色可以同时出现在同一对象上；两者不得互相替代。
- 审核使用 Solid View；保持墙、门、窗、地面和施工上下文正常显示，家具保持黄色。
- 人眼审核必须遵循真实深度遮挡：全部对象的 `show_in_front` 永久关闭，全局 X-ray 关闭；透明度只能表达对象类别或洞口体积，不得使对象置前穿透遮挡。
- 除非某项检查明确需要，不使用 wireframe。
- 审核前由自动化主动选择、聚焦并高亮当前对象组；审核人不负责寻找对象。
- 审核人只需回复“确认”“修改为……”或“暂缓”。

## 6. 完整 15 步执行流程

| # | 阶段 | 操作 | 可验收产物与停止条件 |
| ---: | --- | --- | --- |
| 1 | 固化标准 | 更新本文档中的模度、精度、锚点和例外规则 | 规则有版本记录；不依赖聊天记忆 |
| 2 | 保存基线 | 记录 IFC 哈希、schema、实体数、Root GUID 和主要对象数量；确认 Git 可恢复 | 没有明确恢复点则停止 |
| 3 | 只读盘点 | 审计红色原点、橙色墙、拉伸参数、轮廓、厚度、高度、标高、门洞和相邻墙 | 不修改 IFC；每个对象都有问题分类 |
| 4 | 搜索锚点 | 依次检查现有顶点、物理边中点、整数表面点、墙轴线起点和洞口中心 | 每个候选记录依据、偏移和置信度 |
| 5 | 分类 | 分为可无损重设、需修改几何、需联动构件、无法确定 | 无法确定的对象进入人审，不自动处理 |
| 6 | 推断施工尺寸 | 按 `1 mm` 精度和 `100/50/30/20 mm` 模度生成候选 | 不把旋转或 L 形包围盒投影直接当墙长 |
| 7 | Blender 关系审核 | 恢复标准显示，仅选择并展示存在设计留缝迹象或其他语义多解的候选对象及联动关系 | 默认相邻墙连续无缝；人眼只确认整体位置、拓扑和语义；亚毫米对齐、尺寸和接头残差由机械计算验收 |
| 8 | 第一写入批次 | 从简单直墙开始；联动门洞、门窗和相邻墙 | 单一 IFC 写入者；不处理暂缓对象 |
| 9 | 重设原点 | 几何归整后将原点无损设置到已确认整数锚点 | 原点必须真实位于指定几何特征上 |
| 10 | 保存与重载 | 保存 IFC，读取磁盘验证，并 reload/rebuild Blender | 超时不能直接判失败；必须验证磁盘 |
| 11 | 机械 QA | 检查 schema、GUID、数量、墙连接、洞口包含、空间闭合、缝隙、重叠和非预期变化 | 任一高风险检查失败则停止 |
| 12 | 关系 QA | 恢复标准视图并主动高亮修改对象，检查外轮廓和室内关系 | 已确认的整体关系不重复要求人眼判断；出现施工尺寸或语义多解时才转人审 |
| 13 | 提交批次 | IFC、决策记录和对应 QA 证据形成边界清晰的提交 | 不混入无关变更 |
| 14 | 扩展处理 | 按墙体、门窗、Space、地面、天花、固定家具重复步骤 3–13 | 每一类独立通过验收门 |
| 15 | 最终退出 | 红/橙标记清零，或剩余对象全部有确认的例外和责任状态 | 施工参数、锚点和跨图数据一致 |

## 7. 第一批墙体 dry-run 分组

当前墙体审核集合为 40 面橙色墙：12 面存在非整数拉伸深度，33 面存在非整数水平包围尺寸，其中 5 面同时存在两类问题。dry-run 必须把它们分成：

1. 可直接从解析几何得到施工控制参数的简单直墙。
2. 必须联动门洞、门窗或相邻墙的宿主墙。
3. 包围盒受旋转、L 形或组合几何影响，不能直接推断墙长的墙。
4. 缺少可靠依据，必须由审核人决定的墙。

每个自动推断记录：`GlobalId`、当前值、候选值、差值、锚点类型、联动对象、依据、置信度、是否需要人审和状态。机器结果写入 `build/coordinate-normalization/`，审核在 Blender 中完成。

复杂曲线或圆弧的离散采样点允许保留非整数坐标；它们不是施工长度语义，不得逐点取整。多个产品复用同一 Profile 时不得原位改写共享 Profile；应改用刚体锚点、独立表示或登记受控例外，并以全模型几何指纹排除连带变化。

## 8. 每批验证门

1. 写前和写后 schema 一致，Root GlobalId 集合不变；新增或移除实体必须有明确解释。
2. 未批准对象的世界几何不得发生超出验证容差的变化。
3. 已批准墙体的轴线端点、厚度、高度和标高与候选值一致。
4. 门洞仍被宿主墙包含，门窗仍与洞口和完成面一致。
5. 相邻墙无新增缝隙、重叠或失去连接。
6. Space 边界闭合；未批准的外轮廓顶点保持不变。
7. 原点为整数毫米，并位于约定的几何锚点上；若整数化会使相邻墙接缝超过 `0.1 mm`，必须保留原几何关系并登记受控例外。
8. IFC 保存、磁盘哈希、Blender reload/rebuild、对象数量和截图证据齐全。

门窗原点若已机械证明位于宿主洞口门槛/窗台处的底边中点，允许保留该边缘锚点并随洞口整体归整；无需为了改成洞口中心而重写门窗表示。缺少 `IfcRelFillsElement` 的门窗不得假定宿主或锚点，转 A-104 建模/语义确认。

## 9. 当前停止门

C002 已完成：20 条 GridAxis、22 个 Space、88 面墙及全部 16 个有宿主洞口的门窗关键锚点已机械归整。当前剩余 3 个红点均为无 `IfcRelFillsElement` 的 IfcDoor：`1TW6$_GfnABRZusYvx0zZG`、`2D5BPoo2XFSvhTdfPenCh7`、`0zjVS5FBbBewgUkk0fdfiv`，转 A-104 建模/语义确认。墙 `3hKNxGFpT1NPjsqhFgHhOQ` 已在 C003 通过人审后归整到整数原点和精确正交坐标系；共享曲线 Profile 未原位改写，宿主 Opening `2XVxPKYhfB79rI5US7KTf3` 世界几何保持不变，目标墙相关共面与接缝候选全部满足 `0.1 mm`。

C002 的完成仅指上述关键锚点批次，不代表全部施工控制尺寸均已收口。C003 使用 `pipeline/scripts/geometry_alignment_audit.py` 分别检查 ObjectPlacement、世界坐标几何、近共面边和墙体接缝；`--tolerance-mm` 为可传入的毫米公差，默认 `0.1`。当前全模型 183 条墙体共面关系与 165 个非平行墙体接头全部满足 `0.1 mm`。绝对邻近扫描只产生关系候选；相邻墙默认连续无缝，只有模型关系或设计用途明确提示可能留缝时才转设计人确认。每次几何修改必须同时提供 Git 或候选 IFC 基线差分，区分纯原点重设、整体移动和局部几何修改。

当前已完成 73 个对象的既有纯原点归整、剩余 61 个固定家具锚点批次、70 个 `RA.LP` 灯具安装中心批次、7 个插座安装面中心批次，以及 `HD01 / WD01 / OV01` 三个固定设备纯原点批次。家具角色表已将 89 件家具分成 19 件活动家具受控例外与 70 件固定安装构件；70/70 固定构件原点现均在整数毫米锚点上。79/79 灯具安装中心、7/7 安全插座和三个固定设备锚点已通过 `0.1 mm` 门槛。`HD01 / WD01 / OV01` 的 ObjectPlacement 已改到自身现有底面整数点，世界几何、柜体/墙面接触和非目标对象全部不动。此后又完成 C003 的 72 对象纯原点批次：5 个共享组 Opening、57 个 IfcCovering 与 10 个 IfcSlab；完整原子链比较 138 个相关对象，其中 72 个只改变 ObjectPlacement 且世界几何保持，66 个完全不变。写入产生的 5 个 `10^-19～10^-23` 正交 Direction 尾差随后已清除；只改变 5 个 IfcDirection，全模型最大世界顶点变化 `0.0 mm`。`AC700` 已按安装便利原则采用上安装面中心 `2884/-2783/2667 mm`；下检修面只承担检修净空语义。随后 5 个共享 Opening 组宿主 Covering 已用专用原子候选重设到现有表面整数锚点，7 个直属 Opening 的 4 个唯一 placement 组一次性恢复，共享关系与世界 Boolean 几何保持。两块 Geberit 115.770 冲水面板已采用朝向管井墙的安装面中心作为整数锚点；最大单轴移动 `0.306145 mm`，接触与相交关系保持，错误的 `WCSEAT` 语义留待整批洁具语义收口。最新 8 对象原子批次又完成 5 个 `WW20 CLADDING`、2 个主卧飘窗组合件和 1 个 Cabinet 组合件的纯原点写入；目标和全部非目标 placement、世界几何、组合关系与 Root 集合均通过 `0.1 mm`，PVC110 六根立管相对写前基线变化 `0.0 mm`。磁盘 IFC SHA-256 为 `bd2f0290d088663b65d8e19e185a03457a93db1c04ebf7ebc0001ccd6c742c2a`。原始 308 个超差原点现剩 220 个：114 个随宿主 Opening、19 件活动家具、4 个受控插座和 2 个 PVC110 组合对象由已实施规则关闭；其余 81 个未具备唯一施工锚点的对象已按精确 GlobalId 分配给 A104、A105、WFIN、PLUM、ELEC、RCP1、INT1、DET1。C003 不再有未分类对象或待人审决定；提交前退出审计为 `13 pass / 0 in_progress / 2 pending / 0 blocked`，两个 pending 均由同一未提交状态产生。

剩余 13 个超差 `IfcFlowSegment` 家族对象（7 个直接 `IfcFlowSegment`、6 个 `IfcPipeSegment`）必须先运行 `pipeline/scripts/flow_segment_centerline_audit.py`。脚本用可传入的 `--connection-tolerance-mm`（默认 `0.1`）区分“同一产品中的断开管段”和“在既有网格顶点真实相接的多组件管路”，同时记录 IFC DistributionPort 是否存在。PCA 推导的端面中心只作为中心线证据，不能单独授权写入。两个无类型 PVC110 对象各自保留 3 根由原公寓设计确定的独立立管；流水线只读识别 6 个分支，正式 IFC 禁止拆分、移动、合并或重构。DN75 的两个网格组件共享同一顶点，应保留为一条连续管路。两个 `TEE01` `IfcPipeSegment` 虽各有两个相距约 `1.019 mm` 的网格组件，但它们均关联同一个明确 `IfcPipeSegmentType`，是两件三通产品实例而非管段打包，必须各自保留为一个产品；其安装/连接锚点仍归洁具粗装基准收口。

最新正式写入的原子批次覆盖两个共享 Opening 组、57 个 IfcCovering 和 10 个 IfcSlab，共 72 个原点。目标均使用既有物理边或轴向三角面上的整数点；局部表示施加逆变换，直属 Opening 保持世界 placement。72 对象写入时正式 IFC 与批准候选逐字节一致；随后只清理该写入产生的 5 个正交 Direction 尾差，世界几何不变。墙体共面 183/183、接头 165/165 均通过 `0.1 mm`。5 个与共享 Opening 组耦合的 IfcCovering 宿主未混入通用表面批次，继续由专用宿主—Opening 原子候选处理；它们不是受控例外。

### 9.1 当前归整例外与非例外边界

下列 9 条为当前必须保留的受控例外、非整数设计值或经确认的组合建模方式。它们不是“漏改”；每条都必须在权威决策表中保持唯一、已实施的记录，并由退出审计检查。

| 类别 | 精确范围或允许值 | 保留原因 | 权威记录 |
| --- | --- | --- | --- |
| 拓扑敏感 Direction | `#43042`、`#2081575`、`#2081576`、`#2101029`、`#2101030` | 取正会改变窗或排烟止回阀的 Boolean 三角化 | `COORD-DIRECTION-C003-001` |
| 曲线 Profile 离散点 | 14 面精确 GlobalId 墙 | 圆弧/曲线采样点不是施工控制尺寸，不逐点取整 | `COORD-WALL-C005-CURVE` |
| 飘窗墙 Boolean 安全余量 | 两组连接面 `0.05 mm` | 避免精确共面产生伪面；仍满足 `0.1 mm` 接缝门 | `COORD-WALL-C003-G3` |
| 半毫米板材规格 | `GB01 / WhiteWall = 9.5 mm` | 已确认产品规格；不强制整数毫米 | `COORD-MATL-C003-GB01` |
| 地砖分缝 | `2.0 mm` | 已确认施工分缝，不按共面规则闭合 | `FLOOR-TILE-A105-GROUT-001` |
| 线性地漏留口 | `14.6 mm` | 已确认由 8 个地砖 Opening 控制的 Boolean 留口 | `FLOOR-TILE-A105-DRAIN-001` |
| 活动复杂家具原点 | 19 个精确 GlobalId | 保留产品复杂网格与活动摆放关系 | `FURNITURE-ANCHOR-C003-LOOSE` |
| 插座安装关系 | 4 个精确 GlobalId | 整数吸附会破坏与柜体、墙面或墙体的既有安装接触 | `COORD-SOCKET-C003-CONTROLLED` |
| PVC110 组合建模 | `178mqyyzzFowLcbXcH6prO`、`0bfVg4Ys1CevZs$qxhkXTo` | 每个 `IfcFlowSegment` 保留 3 根由原公寓设计确定的独立立管；6 个分支只读拆分检查，不改正式产品身份或几何 | `COORD-FLOW-C003-PVC110-BUNDLE` |

以下项目经常出现在超差队列中，但不是已批准例外：

| 类别 | 精确范围 | 当前责任状态 | 权威记录 |
| --- | --- | --- | --- |
| 随宿主 Opening | 114 个精确 GlobalId | 委托给宿主 placement 与 Boolean 几何共同验收 | `COORD-OPENING-C003-HOSTREL` |
| 共享 Opening 组 | `0_eLd9WVfFLvk6I8goe8it`、`0vxak9xlD8Z97XrPYRGunC`、`2LCZDONiLBShgK9CpOImZJ`，连同共享表示的 `0faD6FNAT40OGcq7Rvm_OW`、`0GL9sQia9Ch8MrD7xgofsC` | 2 个共享 placement/representation 组已完成纯原点写入；5/5 世界切割几何保持 | `COORD-OPENING-C003-INDEPENDENT` |
| 共享组宿主 Covering | `3UpLXAml9Bufs6wAjUjPJT`、`06wFwLoDD6ie5iCTnc_yad`、`0rwOcjvjn5WBdoq4bxiHSk`、`2roMaBy7v9OvqUBSpWl2Q2`、`37DESmCqj8qAHVsS$K2iyb` | 已用专用宿主—Opening 原子候选完成；7 个直属 Opening 的 4 个 placement 组和共享表示身份保持 | `COORD-SHARED-SURFACE-HOST-C003` |
| 无宿主门 | `1TW6$_GfnABRZusYvx0zZG`、`2D5BPoo2XFSvhTdfPenCh7`、`0zjVS5FBbBewgUkk0fdfiv` | 待 A-104 核定宿主与几何锚点 | `COORD-DOOR-EXC-001` |
| `AC700` | `1PUCikoaP5fgiYt8sJd8$6` | 已按安装便利原则采用上安装面中心；下检修面只用于净空 | `COORD-ELECTRIC-C003-AC700` |

客卫飘窗墙 `3hKNxGFpT1NPjsqhFgHhOQ` 已在 C003 完成归整，不再是当前例外，证据见 `COORD-WALL-C003-CARDINAL`。114 个委托 Opening 不从受控例外数量中计算；它们只从独立处理队列移除。

`pipeline/decisions/p0-review.csv` 是例外、委托项和待决项的唯一权威决策表；`build/` 内的 JSON 只是当前 IFC 哈希的派生证据。退出审计只在决策 ID、scope、status 和精确对象集合同时匹配时接受受控例外，并要求 114 个委托 Opening 的集合精确覆盖当前哈希报告。两个共享组已从超差队列消失；五个耦合宿主继续保持独立待办，不能隐含扣除。

### 9.2 C003 剩余原点责任移交

责任移交不是数值例外，也不授权当前 IFC 写入。它只证明 C003 已经把每个未具备唯一安装锚点的对象交给正确的深化任务；后续任务仍须按 `0.1 mm` 参数门完成候选、写入和 QA。退出审计要求以下 8 行同时存在；未处理行保持 `delegated`，已完成下游候选、写入和 QA 的行改为 `implemented`。当前 7 个未关闭任务精确覆盖扣除受控例外和随宿主 Opening 后的 79 个超差对象：

| 后续任务 | 对象数 | 内容 | 权威记录 |
| --- | ---: | --- | --- |
| A104 | 3 | 无宿主门的洞口、门槛与定位锚点 | `COORD-HANDOFF-C003-A104` |
| A105 | 17 | 16 个 FLOORING Covering 与 1 个 FFL Slab 的完成面锚点 | `COORD-HANDOFF-C003-A105` |
| WFIN | 3 | Painting 与 2 个 WW20BR CLADDING 的墙面放线锚点 | `COORD-HANDOFF-C003-WFIN` |
| PLUM | 30 | 非 PVC110 管段/管件、洁具、地漏和洁具组合件的粗装/连接锚点 | `COORD-HANDOFF-C003-PLUM` |
| ELEC | 9 | 电气设备与开孔 Proxy 的设备/安装中心 | `COORD-HANDOFF-C003-ELEC` |
| RCP1 | 2（已完成） | 风口转角与书架跌级天花已完成纯原点写入；世界几何保持 | `COORD-HANDOFF-C003-RCP1` |
| INT1 | 16 | 固定柜体、台面、管井包板及木作 Proxy 的安装边 | `COORD-HANDOFF-C003-INT1` |
| DET1 | 1 | Glass Railing 的基座、玻璃边与楼板收口锚点 | `COORD-HANDOFF-C003-DET1` |

项目测试必须直接读取上述权威决策表并验证 9 类受控例外全部存在且状态为 `implemented`；任何一行被删除、改错 scope 或退回非实施状态，都必须使退出审计失败。其他设计决定（快装龙骨墙、170 mm 完成面、壁龛/垃圾桶位、厨房移门门垛、待拆墙后补及家具产品来源）继续作为普通决策记录，不得冒充数值例外。
